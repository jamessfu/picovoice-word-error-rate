# Word Error Rate (WER) Visualizer

A small React + TypeScript project to compute and visualize Word Error Rate (WER) between two sentences.

## Setup

Install dependencies:

```bash
pnpm install
```
